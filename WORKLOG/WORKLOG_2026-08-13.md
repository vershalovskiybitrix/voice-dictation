## 2026-08-13

- 03:01 User:
  > Да, давай. И может быть сразу подключим еще читалку, потому что в ней тоже есть большой ряд настроек, которые там нужно располагать и в какой-то единой лодке это оформить, чтобы оно было удобно и эргономично. Подумай хорошо как это сделать красиво, прям классно! Приступай, я пока отойду от компьютера, вернусь, посмотрю на результаты и можешь сделать несколько шагов если это утей, а ну текущая на GitHub запушить, а вроде есть доступ сразу И последующий можешь по шагам сделать прям до конца И все проверим вместе там no final stop или как-то так still используй.

  Assistant:
  > Использую `no-final-during-work` и `resource-mode`: ты отходишь, работа длинная, и просишь не останавливаться на промежуточном финале. Сначала зафиксирую состояние, потом спроектирую окно настроек + TTS как один нормальный интерфейс, реализую слоями и буду проверять после каждого.

  Action: Включён режим User Away. Цель: сначала зафиксировать текущий рабочий слой, затем развивать окно настроек и TTS-провайдеры слоями. Ограничение: не ломать PTT по правому Ctrl и штатный запуск `application/run.bat`.

- 03:18 Assistant action:
  Action: Закоммичен и запушен слой `4a9ebd8 Add tray settings window scaffold and toggle chunking`.
  Result: `compileall` и `selftest.py` прошли; `VOICE_SERVICE_TZ.md` оставлен untracked, потому что содержит секреты.

- 03:23 Assistant action:
  Action: Добавлен слой TTS-настроек: `app/tts.py` с SAPI/RHVoice skeleton, окно переписано на вкладки `Результаты`, `Диктовка`, `Файлы`, `Читалка`, `Провайдеры`.
  Result: SAPI voices найдены: `Microsoft Irina Desktop - Russian`, `Microsoft Zira Desktop - English (United States)`. `compileall` и `selftest.py` прошли.

- 03:34 Assistant action:
  Action: Добавлен статус TTS-кандидатов в CLI/окно и общий `quit_cleanly()` для штатного скрытия tray-иконки.
  Result: `tts_test.py --status` показывает SAPI доступным, RHVoice/Piper/Silero не установленными. `compileall` и `selftest.py` прошли.

- 03:42 Assistant action:
  Action: Добавлены быстрые команды в tray: `Прочитать буфер` и `Тест читалки`.
  Result: `compileall` и `selftest.py` прошли. Новые команды требуют перезапуска приложения для появления в текущем tray-меню.

- 03:55 User:
  > вклад и читалка, и провайдеры как будто бы сыры, и черновики
  > Провайдера чтения утей можно выбирать, но опять же вот эта маленькая саппи и так далее чуть-чуть непонятно
  > Кнопки прочитать и тестовая фраза не работают.
  > ... надо разобраться с этой ерундой и сделать чтобы это было чем-то рабочим

  Action: Переделан UI читалки: человеческие названия провайдеров, динамические настройки под выбранный провайдер, вкладка `Провайдеры` заменена на `Подключения`, настройки моделей Whisper/Yandex/both добавлены в `Диктовку`, кнопка `Выход` убрана из читалки. SAPI переведён на синхронное чтение.
  Result: `compileall`, `selftest.py`, `tts_test.py --status`, `tts_test.py --provider sapi "Проверка."` прошли.

- 03:29 User:
  > Без остановок всё по максимуму делай, подключай!

  Action: Установлен Piper Windows AMD64 в `runtime/tts/piper`, скачана русская модель `ru_RU-irina-medium` из `rhasspy/piper-voices`, добавлен реальный адаптер Piper в `application/app/tts.py`, в окно добавлено поле `Piper.exe`, локальный `runtime/config.json` переключён на `tts_provider = piper`.
  Result: `tts_test.py --status` показывает `OK piper`; тестовая фраза через текущие настройки Piper проигралась; обнаружена и исправлена загрузка `config.json` с UTF-8 BOM через `utf-8-sig`.

- 03:55 Assistant action:
  Action: Подключены дополнительные TTS-провайдеры: Silero через PyTorch Hub (`v5_ru`, голоса `aidar/baya/kseniya/eugene/xenia`) и Yandex SpeechKit TTS через `runtime/.env`. Yandex-секреты перенесены из untracked ТЗ в игнорируемый `.env`, значения в чат и git не выводились.
  Result: `tts_test.py --provider silero ...` и `tts_test.py --provider yandex ...` отработали; `tts_test.py --status` показывает `OK sapi`, `OK piper`, `OK silero`, `OK yandex`. `RHVoice-client` и старый Google-robot не найдены.

- 04:05 Assistant action:
  Action: Добавлен экспериментальный `google_old` без конфликтующей зависимости `gTTS`: прямой запрос к старому Google Translate TTS endpoint, MP3 конвертируется в WAV через `soundfile`, затем проигрывается через `winsound`.
  Result: `pip check` чистый; `tts_test.py --provider google_old ...` отработал; статус показывает `OK google_old`.

- 04:20 User:
  > А можно чтобы он всё-таки выделенный текст например при двойном правом контроле читал или предложи другой вариант как легко, без тыканий по трею указывать текст для прочтения.

  Action: Добавлен сценарий чтения выделенного текста: двойной короткий тап правого Ctrl временно копирует выделение через `Ctrl+C`, восстанавливает прежний буфер обмена и читает текст текущим TTS-провайдером. В трей и окно добавлена команда `Прочитать выделенное`; в окне `Читалка` добавлены настройки хоткея.
  Result: `selftest.py` проверяет, что обычный PTT не сломан, а второй короткий тап вызывает `read_selection`.
